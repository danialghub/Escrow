# 🚀 پروژه Smart Contract + React (Hardhat & React.js)

> یک پروژه فول‌استک بلاکچینی شامل **Smart Contract با Hardhat** و **Frontend با React.js** ✨

---

## 🧱 پیش‌نیازها

قبل از شروع، مطمئن شو موارد زیر روی سیستم نصب هستند:

* ✅ **Node.js** (نسخه 18 یا بالاتر پیشنهاد می‌شود)
* ✅ **npm** یا **yarn**
* ✅ **MetaMask** (به‌صورت Extension مرورگر)
* ✅ یک ترمینال (VS Code Terminal پیشنهاد می‌شود)

برای بررسی:

```bash
node -v
npm -v
```

---

# 🔨 بخش اول: راه‌اندازی و اجرای Hardhat

### 📁 ورود به پوشه Smart Contract

```bash
cd smart_contract
```

---

### 📦 نصب وابستگی‌ها

```bash
npm install
```

یا اگر از yarn استفاده می‌کنی:

```bash
yarn
```

---

### ⚙️ اجرای شبکه لوکال Hardhat

```bash
npx hardhat node
```

🔹 این دستور یک بلاکچین لوکال روی آدرس زیر اجرا می‌کند:

```
http://127.0.0.1:8545
```

> ⚠️ این ترمینال را **باز نگه دار**

---

### 📤 دیپلوی Smart Contract

در یک ترمینال جدید:

```bash
npx hardhat run scripts/deploy.js --network localhost
```

📌 بعد از دیپلوی:

* آدرس قرارداد را کپی کن
* ABI را از مسیر `artifacts/` نگه دار (برای React لازم است)

---

### 🦊 اتصال MetaMask به Hardhat

1. Network جدید بساز:

   * RPC URL: `http://127.0.0.1:8545`
   * Chain ID: `31337`

2. یکی از Private Keyهایی که Hardhat node داده را Import کن 💰

---

# ⚛️ بخش دوم: راه‌اندازی و اجرای React.js

### 📁 ورود به پوشه Frontend

```bash
cd frontend
```

---

### 📦 نصب پکیج‌ها

```bash
npm install
```

---

### 🧠 تنظیم Contract در React

در فایل مربوط به کانفیگ (مثلاً `contract.js` یا `config.js`):

* 🔹 آدرس Contract دیپلوی‌شده
* 🔹 فایل ABI

را قرار بده.

---

### ▶️ اجرای پروژه React

```bash
npm run dev
```

یا:

```bash
npm start
```

سپس پروژه را در مرورگر باز کن:

```
http://localhost:3000
```

---

## 🔗 اتصال React به MetaMask

* MetaMask باید روی **localhost Hardhat network** باشد
* اجازه اتصال سایت به MetaMask را بده
* حالا React مستقیماً با Smart Contract صحبت می‌کند 😎

---

## 🧪 استک تکنولوژی‌ها

* 🧠 **Solidity** – Smart Contract
* 🔨 **Hardhat** – Development Environment
* 🔗 **ethers.js** – ارتباط با بلاکچین
* ⚛️ **React.js** – Frontend
* 🦊 **MetaMask** – Wallet

---

## 💡 نکات حرفه‌ای

* هر تغییر در Contract → نیاز به **Deploy مجدد** دارد
* ABI و Address همیشه باید با هم Sync باشند
* برای Production از شبکه‌های Testnet استفاده کن (Sepolia / Goerli)

---

## ✨ نتیجه

این پروژه یک نمونه **Full Stack Web3** واقعی است که:

> `React → MetaMask → ethers.js → Hardhat → Solidity`

را به هم متصل می‌کند 🔥

---

### 🌟 موفق باشی و Web3 بساز!

اگر خواستی README رو حرفه‌ای‌تر یا انگلیسی کنیم بگو 😎
